#Tuples are aslo used for store multiple items in a single variable
mytuple = ("raju","popi","munni")
print(type(mytuple))

#We can also access items using index [0] print "raju"
print(mytuple[0])

thistuple = ("apple", "banana", "cherry", "orange", "kiwi", "melon", "mango")

#We can access index range wise like index[2] to index[5] ar maje sob gula items display korar jono index[2:5]
# output will be ('cherry', 'orange', 'kiwi')
print(thistuple[2:5])

